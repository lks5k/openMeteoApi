{
  "name": "open-meteo-dashboard",
  "version": "1.0.0",
  "description": "Dashboard del clima con Open-Meteo",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
